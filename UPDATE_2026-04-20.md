# Update Log — 2026-04-20

## สรุปงานที่ทำวันนี้

---

### 1. Cleanup ไฟล์เก่า (ที่ตกลงว่าไม่ใช้แล้ว)

**ลบไฟล์ออก:**
- `services/pdfService.js` — PDF กรมธรรม ไม่ต้องการ
- `services/renewalReminder.js` — ระบบเตือนต่ออายุ ไม่ต้องการ
- `views/admin/quotes.ejs` — หน้าใบเสนอราคา (ระบบเก่า)
- `views/admin/quote_detail.ejs` — หน้าดูรายละเอียดใบเสนอราคา (ระบบเก่า)

**ลบ Routes ออกจาก `routes/admin.js`:**
- `GET /admin/quotes` — list ใบเสนอราคา
- `GET /admin/quotes/export` — export CSV
- `GET /admin/quotes/:id` — ดูรายละเอียด
- `GET /admin/quotes/:id/pdf` — download PDF
- `POST /admin/quotes/:id/status` — อัปเดตสถานะ

---

### 2. แก้ไข `routes/admin.js`

- ลบ `STATUS_MAP` ออกจาก `av()` helper (ไม่มี quotes แล้ว)
- เพิ่ม `compulsory: 'พรบ.'` ใน `TYPE_MAP` (รองรับ insurance_type ครบทุกค่า)
- เขียน `/reports` route ใหม่ทั้งหมด — query จาก `leads` table แทน `quotes`

**Queries ใหม่ใน Reports:**
| Query | ข้อมูล |
|---|---|
| Funnel totals | total leads, LINE clicks, FB clicks, total clicks, converted |
| Monthly 12 เดือน | leads/clicks/converted แยกรายเดือน |
| By Source | organic / line_ads / facebook_ads / google |
| By Insurance Type | class1 / class2plus / class3plus / compulsory |
| Top 8 Brands | ยี่ห้อรถที่ leads เยอะที่สุด |
| Daily 7 วัน | leads + clicks รายวัน |
| Recent 10 Leads | รายการล่าสุด |

---

### 3. แก้ไข `views/admin_layout.ejs`

- ลบ nav item "ใบเสนอราคา" (`/admin/quotes`) ออกจาก sidebar

---

### 4. เขียน `views/admin/reports.ejs` ใหม่ทั้งหมด

Design ใหม่แสดง:
- **4 Stat cards** — Total Leads / LINE Clicks % / FB Clicks % / Converted
- **Bar chart รายเดือน** — 12 เดือนล่าสุด
- **4 Charts** — By Source / By Type / Top Brands / Daily 7 วัน
- **ตาราง Recent Leads** — 10 รายการล่าสุด พร้อม link ไป `/admin/leads`

---

### สถานะระบบหลังอัปเดต

| ส่วนงาน | สถานะ |
|---|---|
| Public form + Result page | ✅ |
| Lead capture + UTM tracking | ✅ |
| LINE bot (human_first mode) | ✅ |
| Admin dashboard | ✅ |
| Admin leads management | ✅ |
| Admin reports | ✅ (ใหม่วันนี้) |
| Admin LINE chat | ✅ |
| Scraper | ✅ |
| Cleanup ระบบเก่า | ✅ (ใหม่วันนี้) |

---

### ยังต้องทำก่อน Deploy Production

1. ใส่ `ANTHROPIC_API_KEY` ใน `.env`
2. ตั้ง LINE OA URL + Facebook URL ใน Admin → Settings
3. รัน migrations ทั้งหมดบน Production DB
